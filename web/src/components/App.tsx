import React from 'react';
import './App.css';
import ContextMenu from './ContextMenu';

const App: React.FC = () => {
  return (
    <div className="nui-wrapper">
      <ContextMenu />
    </div>
  );
};

export default App;
